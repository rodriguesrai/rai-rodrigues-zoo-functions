const data = require('../data/zoo_data');

// const ScheduleAnimals = (animal) => {
//   const animalSpecies = data.species.find((species) => species.name === animal)
//   return animalSpecies.availability;
// };

const getSchedule = (scheduleTarget) => {
  // {
  //  Monday:
  //  Tuesday:
  //  Wednesday:
  //  Thursday:
  //  Friday:
  //  Saturday:
  //  Sunday:
  // }
};

module.exports = getSchedule;
